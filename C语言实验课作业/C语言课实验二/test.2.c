#include<stdio.h>
int main(){
    int a[1][1];
    int i,j;
    printf("请输入两个分数\n");
    scanf("%d/%d,%d/%d",&a[0][0],&a[0][1],&a[1][0],&a[1][1]);
    int (*p)[1]=a;
    if (*(*(p))*(*(p+1)[1])<(*(p)[1])*(*(p+1)[0]))
    {
        printf("%d/%d<%d/%d\n",*(p)[0],*(*(p)+1),*(p+1)[0],*(*(p+1)+1));
    }
    else if (*(*(p))*(*(p+1)[1])>(*(p)[1])*(*(p+1)[0]))
    {
        printf("%d/%d>%d/%d\n",*(p)[0],*(*(p)+1),*(p+1)[0],*(*(p+1)+1));
    }else{
        printf("这两数相等\n");
    } 
}